# Stele product repository

Follow [AGENTS.md](AGENTS.md). Keep the Go verifier deterministic, dependency-light, and private under `internal/stele`; keep example applications in `stele-examples`.

Before review, run `npm run verify`. After documentation changes, also run `npm run docs:build`.
